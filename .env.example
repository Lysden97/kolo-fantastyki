# Local development. Generate your own SECRET_KEY and database password.
DEBUG=True
SECRET_KEY=
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:8001,http://127.0.0.1:8001
DATABASE_ENGINE=postgresql
DATABASE_NAME=kolo_fantastyki
DATABASE_USER=kolo_fantastyki
DATABASE_PASSWORD=
DATABASE_HOST=localhost
DATABASE_PORT=5432
# Demo messages are printed locally, never sent to the real club.
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=demo@example.invalid
CONTACT_RECIPIENT=demo@example.invalid
EMAIL_HOST=localhost
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
# Only enable behind a trusted proxy which overwrites forwarding headers.
TRUST_PROXY_HEADERS=False
